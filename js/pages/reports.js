/* FinCraft · pages/reports.js — barrel re-export.
   Implementation now lives in ./reports/. Kept as a thin barrel so every
   existing `import ... from 'reports.js'` elsewhere in the app still works. */
export * from './reports/index.js';
