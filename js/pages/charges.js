/* FinCraft · pages/charges.js — barrel re-export.
   Implementation now lives in ./charges/. Kept as a thin barrel so every
   existing `import ... from 'charges.js'` elsewhere in the app still works. */
export * from './charges/index.js';
