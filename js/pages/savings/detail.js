/* FinCraft · pages/savings/detail.js — barrel re-export.
   Implementation now lives in ./detail/. Kept as a thin barrel so every
   existing `import ... from 'detail.js'` elsewhere in the app still works. */
export * from './detail/index.js';
