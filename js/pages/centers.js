/* FinCraft · pages/centers.js — barrel re-export.
   Implementation now lives in ./centers/. Kept as a thin barrel so every
   existing `import ... from 'centers.js'` elsewhere in the app still works. */
export * from './centers/index.js';
