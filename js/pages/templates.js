/* FinCraft · pages/templates.js — barrel re-export.
   Implementation now lives in ./templates/. Kept as a thin barrel so every
   existing `import ... from 'templates.js'` elsewhere in the app still works. */
export * from './templates/index.js';
