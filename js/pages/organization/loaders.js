/* FinCraft · pages/organization/loaders.js — barrel re-export.
   Implementation now lives in ./loaders/ (2nd-pass split).
   Kept as a thin barrel so sibling files' `from './loaders.js'` imports still work. */
export * from './loaders/index.js';
