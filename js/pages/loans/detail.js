/* FinCraft · pages/loans/detail.js — barrel re-export.
   Implementation now lives in ./detail/ (2nd-pass split).
   Kept as a thin barrel so sibling files' `from './detail.js'` imports still work. */
export * from './detail/index.js';
