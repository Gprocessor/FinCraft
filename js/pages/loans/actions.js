/* FinCraft · pages/loans/actions.js — barrel re-export.
   Implementation now lives in ./actions/ (2nd-pass split).
   Kept as a thin barrel so sibling files' `from './actions.js'` imports still work. */
export * from './actions/index.js';
