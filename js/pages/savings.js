/* FinCraft · pages/savings.js — barrel re-export.
   Implementation now lives in ./savings/ (shared, list/detail/actions, index).
   Kept as a thin barrel so router.js's `import('./pages/savings.js')` still works. */
export * from './savings/index.js';
