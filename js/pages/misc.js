/* FinCraft · pages/misc.js — barrel re-export.
   Implementation now lives in ./misc/. Kept as a thin barrel so every
   existing `import ... from 'misc.js'` elsewhere in the app still works. */
export * from './misc/index.js';
